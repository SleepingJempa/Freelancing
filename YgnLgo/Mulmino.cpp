#include <iostream>
#include <vector>

using namespace std;

int segs(vector<int> &a, int i) {
    bool smaller = a[i] < 
}

int main() {
    int n;
    vector<int> a(n);

    for(int i = 0; i < n; i++) {
        cin >> a[i];
    }


}